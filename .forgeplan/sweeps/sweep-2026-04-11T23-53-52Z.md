# Sweep Report — Pass 1

**Generated:** 2026-04-12T00:00:00Z
**Operation:** deep-building
**Pass:** 1
**Tier:** LARGE
**Agents dispatched:** sweep-adversary, sweep-contractualist, sweep-pathfinder, sweep-structuralist, sweep-skeptic
**Failed agents:** none

## Summary

| Severity | Count |
|----------|-------|
| HIGH     | 16    |
| MEDIUM   | 26    |
| LOW      | 2     |
| **Total** | **44** |
| needs_manual_attention | 1 |

---

## Findings by Node

### core-infrastructure

**F1** `HIGH/98` — `main.py`: 6 of 9 module routers not registered — `clinical`, `matching`, `outreach`, `outcomes`, `analytics`, `admin` all return 404. 77 acceptance criteria completely unreachable. Only `auth_router`, `facilities_router`, `intake_router` are mounted.
*Reported by: adversary, contractualist, pathfinder, structuralist, skeptic*

**F2** `HIGH/97` — `requirements.txt`: Missing runtime dependencies — `haversine` (matching-module geo scoring), `zipcodes` (matching-module zip lookup), `Jinja2` (outreach-module template rendering). Fresh install raises `ImportError` and server won't start.
*Reported by: adversary, contractualist, structuralist, skeptic*

**F3** `HIGH/95` — `main.py`: Router prefix convention inconsistent — 3 different patterns observed across mounted modules (no prefix on some, `/api/v1` on others, inconsistent mounting). Creates route ambiguity and makes it impossible to predict registration order.
*Reported by: structuralist*

**F4** `HIGH/92` — `placementops/core/state_machine.py`: Closed-case guard not enforced as central middleware — each module independently reimplements the guard. A module that forgets to call it silently allows mutations to closed cases.
*Reported by: adversary, skeptic*

**F5** `MEDIUM/95` — `placementops/core/database.py`: `TenantMixin` class defined but never inherited by any ORM model — dead code providing false sense of tenant isolation at ORM layer.
*Reported by: structuralist*

**F6** `MEDIUM/92` — `docker-compose.yml` line 26: `SUPABASE_JWT_SECRET: "dev-secret-change-in-production"` committed with no startup guard. No `APP_ENV` check prevents this from being used in production.
*Reported by: structuralist*

**F7** `MEDIUM/90` — `placementops/core/state_machine.py`: `transition_case_status` loads case by `id` only with no `organization_id` filter — cross-tenant status transition possible if case ID is known.
*Reported by: adversary, skeptic*

**F8** `MEDIUM/88` — `.env.example`: `APP_ENV=development` default — exposes Swagger/ReDoc docs at `/docs` and `/redoc` if `.env.example` is copied verbatim without change.
*Reported by: adversary*

**F9** `MEDIUM/86` — `placementops/core/` PHI log filter: Redacts field names in log output but not field values — PHI values (patient names, DOB, MRN) may appear unredacted in logs.
*Reported by: adversary*

---

### auth-module

**F10** `HIGH/95` — `placementops/core/auth.py`: JWT algorithm confusion — trusts the unverified `alg` header field; unknown algorithms fall through to HS256. An attacker who obtains the HMAC secret can forge ES256-signed tokens by changing the header.
*Reported by: adversary*

**F11** `HIGH/93` — `placementops/modules/auth/rate_limiter.py`: `X-Forwarded-For` trusted without proxy allowlist validation — rate limit bypassable by adding a spoofed `X-Forwarded-For` header from any client.
*Reported by: adversary*

**F12** `MEDIUM/91` — `placementops/modules/auth/rate_limiter.py`: Unbounded `defaultdict(deque)` grows without eviction — memory exhausted under sustained DoS/brute-force attack.
*Reported by: adversary*

**F13** `MEDIUM/90` — `placementops/core/auth.py`: Logout silently returns HTTP 200 when Supabase is unreachable — token is not invalidated server-side and client receives no error.
*Reported by: adversary*

**F14** `MEDIUM/85` — `placementops/core/auth.py`: `role_key` fallback reads `payload.get("role")` from JWT root claim — a stale token with an elevated root claim can bypass `app_metadata` RBAC checks.
*Reported by: adversary*

---

### intake-module

**F15** `HIGH/99` — `placementops/modules/intake/router.py`: Double-prefix bug — `APIRouter(prefix="/api/v1")` combined with `app.include_router(..., prefix="/api/v1")` in main.py → all intake routes resolve at `/api/v1/api/v1/cases`. All intake endpoints are effectively 404.
*Reported by: contractualist, pathfinder*

**F16** `HIGH/92` — `placementops/modules/intake/router.py` line 355: File upload reads full request body into memory *before* checking the 10 MB limit — a 1 GB upload causes OOM/DoS before the guard fires.
*Reported by: adversary*

**F17** `MEDIUM/95` — `placementops/modules/intake/models.py`: `HospitalReference` ORM missing `city` and `state` columns — these fields are used in query filters and response schemas but do not exist in the table.
*Reported by: contractualist*

**F18** `MEDIUM/87` — `placementops/modules/intake/router.py`: Content-type validation trusts the client-supplied MIME header instead of inspecting magic bytes — malicious uploads bypass the type check.
*Reported by: adversary*

**F19** `MEDIUM/80` — `placementops/modules/intake/service.py`: Phone manual/task bypass uses separate database commits instead of a single flush + atomic commit — partially-applied state possible on failure mid-sequence.
*Reported by: skeptic*

**F20** `LOW/82` — `placementops/modules/intake/`: LIKE search queries do not escape `%` and `_` metacharacters — SQL LIKE pattern injection expands search scope beyond intended results.
*Reported by: adversary*

---

### clinical-module

**F21** `HIGH/95` — `placementops/modules/clinical/schemas.py`: `ClinicalAssessment` Pydantic schemas use facility-centric field names (`accepts_trach`, `accepts_vent`, `accepts_oxygen_therapy`, `accepts_wound_vac`, `accepts_hd`) while canonical shared types (`src/shared/types/index.ts`) define patient-centric names (`trach`, `vent`, `oxygen_required`, `wound_vac_needs`, `dialysis_required`). API contract broken between backend and frontend.
*Reported by: contractualist*

---

### outreach-module

**F22** `MEDIUM/90` — `placementops/modules/outreach/template_renderer.py`: `autoescape=False` passed to `SandboxedEnvironment` — stored XSS possible if rendered output is delivered as HTML response.
*Reported by: adversary*

**F23** `MEDIUM/89` — `placementops/modules/outreach/`: Template body content stored verbatim in audit event payloads — PHI interpolated into outreach templates appears unredacted in audit log.
*Reported by: adversary*

**F24** `MEDIUM/88` — `placementops/modules/outreach/service.py`: Does not validate `FacilityMatch.selected_for_outreach` before creating outreach action — allows outreach to be created for facilities that were never selected during the matching phase.
*Reported by: contractualist*

---

### outcomes-module

**F25** `HIGH/92` — `placementops/modules/outcomes/service.py` line 285: `facility_id is not None` guard logic is inverted — when `facility_id=None`, the entire sent-outreach validation is bypassed. An "accepted" placement outcome can be recorded with no associated facility.
*Reported by: skeptic, pathfinder*

**F26** `MEDIUM/92` — Route collision: both `outcomes-module` and `intake-module` define `POST /cases/{id}/status-transition`. FastAPI registers first match; the second is silently unreachable.
*Reported by: contractualist*

**F27** `MEDIUM/90` — `placementops/modules/outcomes/`: `pending_review` appears in TypeScript `OutcomeType` union but is absent from the Python `VALID_TYPES` list — Python rejects submissions frontend considers valid.
*Reported by: contractualist*

**F28** `MEDIUM/88` — `tests/outcomes/test_outcomes.py` or `test_timeline.py`: Missing test for "placed" audit event emission (outcomes acceptance criterion AC14).
*Reported by: pathfinder*

---

### analytics-module

**F29** `MEDIUM/88` — `frontend/src/app/analytics/AnalyticsDashboard.tsx` line 26: Fetches `/api/v1/analytics/summary` — this endpoint does not exist; spec defines `/api/v1/analytics/dashboard`. Analytics dashboard always returns 404.
*Reported by: pathfinder*

**F30** `MEDIUM/88` — `placementops/modules/analytics/service.py`: SLA query logic and row-processing duplicated between `get_operations_queue` and `get_manager_summary` — divergence risk if one is updated without the other.
*Reported by: structuralist*

**F31** `MEDIUM/85` — `frontend/src/app/analytics/`: Analytics loading state renders plain text, not a skeleton UI — jarring layout shift on slow connections.
*Reported by: pathfinder*

---

### admin-surfaces

**F32** `HIGH/99` — `placementops/modules/admin/router.py` line 50: Double-prefix bug — same pattern as intake router. Admin routes resolve at `/api/v1/api/v1/admin`. All admin endpoints are 404.
*Reported by: contractualist*

**F33** `HIGH/98` — `placementops/modules/admin/schemas.py`: `OrgSettingsResponse` declares `settings_json` and `updated_at` fields but the `Organization` ORM model has neither column — all org-settings reads produce a Pydantic validation error.
*Reported by: contractualist*

**F34** `MEDIUM/90` — `placementops/modules/admin/index.py`: Re-exports router but is never imported anywhere in the codebase — dead code.
*Reported by: structuralist*

---

### frontend-app-shell

**F35** `HIGH/96` — `frontend/src/`: References status string `"ready_for_clinical_review"` which does not exist in the canonical 14-status state machine — UI enters broken state when this path is reached.
*Reported by: structuralist*

**F36** `HIGH/93` — `frontend/src/app/`: Most page routes are pure client components with no server-side auth gate — unauthenticated users can load the page shell before the client-side redirect fires (flash of unauthorized content).
*Reported by: structuralist*

**F37** `HIGH/93` — `frontend/src/components/ClinicalAssessmentForm.tsx`: Zod schema uses patient-centric field names (`trach`, `oxygen_required`, `dialysis_required`) while backend Pydantic schemas expect facility-centric names (`accepts_trach`, `accepts_oxygen_therapy`, `accepts_hd`) — all clinical boolean fields silently default to `false` on submission; clinical data is never persisted.
*Reported by: contractualist, pathfinder, skeptic*

**F38** `HIGH/92` — `frontend/src/client/index.ts`: API client is a placeholder using raw `fetch()` with no Supabase auth token attached — every authenticated API call returns 401 Unauthorized.
*Reported by: pathfinder*

**F39** `MEDIUM/92` — `frontend/jest.config.ts` and `frontend/jest.config.js` both exist with identical config; `package.json` points to `jest.config.js`; `jest.config.ts` is dead code and will not be picked up by the test runner.
*Reported by: structuralist*

**F40** `MEDIUM/91` — `frontend/src/`: SLA computation uses different thresholds and different timestamp sources than the backend analytics module — SLA status shown in the UI does not match what the backend computes.
*Reported by: structuralist*

**F41** `MEDIUM/90` — `frontend/src/components/ActivityTimeline.tsx` lines 26-35: Uses emoji characters (`"🔄"`, `"✨"`, `"🩺"`) as UI icons — violates frontend-design skill rules (no emoji in interface copy).
*Reported by: pathfinder*

**F42** `MEDIUM/87` — `frontend/src/app/intake/import/page.tsx` line 116: Polls `/api/v1/intake/imports/{jobId}` — backend routes this at `/api/v1/imports/{id}` (extra `intake/` segment is wrong). Import job status polling always returns 404.
*Reported by: pathfinder*

**F43** `MEDIUM/83` — `frontend/src/components/DraftEditor.tsx`: `canApprove` condition includes only `"admin"` and `"manager"` — spec requires `"placement_coordinator"` can also approve outreach drafts. Coordinators see no Approve button.
*Reported by: pathfinder*

**F44** `LOW/88` — `frontend/src/components/StatusBadge.tsx`: Redeclares `CaseStatus` type locally instead of importing from `src/shared/types/index.ts` — creates drift risk if statuses change.
*Reported by: contractualist*

---

## Needs Manual Attention

**MA-1** `MEDIUM/85` — `[project]` Inconsistent pagination response shape across modules — some return `{items, total, page, pages}`, others return `{data, count}`. Cross-cutting API contract decision required before frontend client generation.
*Reported by: contractualist*

---

## Agent Raw Finding Counts (before dedup)

| Agent | Raw findings | After dedup |
|-------|-------------|-------------|
| sweep-adversary | 16 | contributed to 16 |
| sweep-contractualist | 14 | contributed to 17 |
| sweep-pathfinder | 13 | contributed to 11 |
| sweep-structuralist | 12 | contributed to 11 |
| sweep-skeptic | 18 | contributed to 8 |
| **Total raw** | **73** | **44 unique** |
