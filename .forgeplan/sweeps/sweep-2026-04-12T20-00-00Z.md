# Sweep Report — Pass 1

Model: claude
Timestamp: 2026-04-12T20:00:00Z
Agents: adversary, contractualist, pathfinder, structuralist, skeptic
Total findings: 45 node-scoped + 4 project-level
High: 17 | Medium: 24 | Low: 4
Filtered low-confidence: 0
Project-level (manual): 4

---

## Findings by Node

### frontend-app-shell (24 findings)

- F1 [architecture] HIGH (97): API client uses `supabase.auth.getSession()` which reads from local cache without re-validating with auth server — violates AC5; revoked tokens still forward as Bearer credentials — `frontend/src/client/index.ts:40`
- F2 [api-contracts] HIGH (97): Missing per-case outreach GET route — frontend fetches `/api/v1/cases/${caseId}/outreach` but no such route exists on backend — `frontend/src/app/cases/[case_id]/page.tsx:93`
- F3 [api-contracts] HIGH (97): Three outreach action mutation URLs missing `-actions` segment — frontend uses `/outreach/{id}/approve|mark-sent|cancel` but backend registers `/outreach-actions/{id}/...` — `frontend/src/app/cases/[case_id]/page.tsx:236,243,250`
- F4 [api-contracts] HIGH (97): Outreach page fetches `/api/v1/outreach?limit=50` but backend route is `GET /api/v1/queues/outreach` — always 404 — `frontend/src/app/outreach/page.tsx:94`
- F5 [api-contracts] HIGH (97): Template fetch URL is `/api/v1/outreach/templates` but correct route is `/api/v1/templates/outreach` — plus admin and outreach modules return different keys (`items` vs `templates`) — `frontend/src/app/cases/[case_id]/page.tsx:96`
- F6 [type-consistency] HIGH (97): Dashboard KPI component expects `active_cases, cases_placed_today, avg_placement_days, pending_approvals` but backend `DashboardReport` returns `total_cases, cases_by_status, placement_rate_pct, stage_metrics` — all 4 fields undefined, all KPIs show "—" — `frontend/src/app/dashboard/DashboardKPIs.tsx:11`
- F7 [api-contracts] HIGH (95): Match list response key is `matches` (backend) but frontend destructures `items` — list always empty — `frontend/src/app/cases/[case_id]/page.tsx:90`
- F8 [api-contracts] HIGH (95): Create outreach URL is `/cases/${caseId}/outreach` but backend route is `/cases/{case_id}/outreach-actions` — always 404 — `frontend/src/app/cases/[case_id]/page.tsx:225`
- F9 [api-contracts] HIGH (95): Assessment PATCH sends to `/cases/${caseId}/assessments` but backend PATCH route is `/assessments/{assessment_id}` — 404 on every existing-assessment save — `frontend/src/app/cases/[case_id]/page.tsx:212`
- F10 [user-flows] HIGH (91): Admin audit tab always empty — `loadCaseData` checks `userRole === "admin"` at mount time but `userRole` is `null` (resolved async); `userRole` not in `useEffect` deps so re-run never happens — `frontend/src/app/cases/[case_id]/page.tsx:79`
- F11 [user-flows] HIGH (88): CaseTable and FilterBar share nuqs keys with no page prefix — navigating intake→queue carries over status filters; users see contaminated filter state with no indication why — `frontend/src/components/CaseTable.tsx:26`
- F12 [type-consistency] MEDIUM (92): Assessment list response key is `assessments` (backend `AssessmentListResponse`) but frontend accesses `assessmentData.items[0]` — always undefined, throws TypeError at runtime — `frontend/src/app/cases/[case_id]/page.tsx:126`
- F13 [type-consistency] MEDIUM (90): Cases list response key is `cases` (backend `PaginatedCasesResponse`) but intake page reads `data.items` — table always empty — `frontend/src/app/intake/page.tsx:128`
- F14 [api-contracts] MEDIUM (90): Frontend calls `GET /api/v1/cases/check-duplicate?...` which doesn't exist — backend embeds duplicate check in POST response as `duplicate_warning` field; client-side pre-check always 404 — `frontend/src/app/intake/page.tsx:152`
- F15 [type-consistency] MEDIUM (92): Frontend `DuplicateWarning` interface has `{matching_cases, patient_name, dob}` but backend schema has `{existing_case_id, patient_name, dob, hospital_id, current_status}` — entirely different shapes — `frontend/src/app/intake/page.tsx:50`
- F16 [api-contracts] MEDIUM (88): Select toggle sends JSON body `{selected_for_outreach: selected}` but backend endpoint takes no body — also toggle semantics mismatch (second click flips selection back) — `frontend/src/app/cases/[case_id]/page.tsx:185`
- F17 [type-consistency] MEDIUM (85): `FacilityCardData` expects `facility_name, facility_type, distance_miles` but `FacilityMatchResponse` only returns `facility_id` — facility display always blank — `frontend/src/components/FacilityCard.tsx:12`
- F18 [user-flows] MEDIUM (85): Import page polling uses stale closure — `pollJobStatus` useCallback captures stale `pollInterval` ref, orphaned intervals fire after job completes — `frontend/src/app/intake/import/page.tsx:112`
- F19 [user-flows] MEDIUM (87): DraftEditor "Submit for Approval" path for existing drafts drops `draft_subject` field — email outreach loses subject on re-submit — `frontend/src/components/DraftEditor.tsx:211`
- F20 [user-flows] MEDIUM (82): OrgSettingsTab initializes `orgName` to `""` with no fetch on mount — admin always sees blank input, cannot see current org name — `frontend/src/app/admin/page.tsx:269`
- F21 [user-flows] MEDIUM (80): ClinicalAssessmentForm dual submit buttons race — failed validation leaves stale `review_status` in form state; subsequent click of other button submits with wrong status — `frontend/src/components/ClinicalAssessmentForm.tsx:487`
- F22 [test-quality] MEDIUM (86): CaseTable.test.tsx tests local constant `MANUAL_TABLE_OPTIONS`, not actual component — if `manualSorting` changed to false, test still passes — `frontend/src/components/__tests__/CaseTable.test.tsx:11`
- F23 [code-quality] MEDIUM (91): `getRoleLanding()` function duplicated identically in `layout.tsx:16` and `page.tsx:7` — two sources of truth for role routing
- F24 [documentation] LOW (85): Frontend API client is a placeholder pending `npm run generate-client` against running FastAPI — all pages use untyped fetch; no README or CI check for staleness — `frontend/src/client/index.ts:1`

### core-infrastructure (4 findings)

- F25 [auth-security] HIGH (95): `auth_ctx.role_key` in `AuthContext` is sourced from JWT claim (`auth.py:161`), not DB; service-layer calls to `transition_case_status()` in outreach/service.py:338,539, matching/service.py:429, outcomes/service.py:345,361,511 pass the JWT role instead of the DB-authoritative role — demoted users retain transition privileges until JWT expires — `placementops/core/auth.py:161`
- F26 [database] MEDIUM (88): `0002_rls_policies.py` creates RLS policies for `placement_outcomes` referencing `organization_id` column which does not exist on the table (confirmed in `0001_initial_tables.py:310-321`) — Alembic upgrade will fail with PostgreSQL error, leaving `placement_outcomes` without RLS protection — `alembic/versions/0002_rls_policies.py:19`
- F27 [code-quality] MEDIUM (88): `TenantMixin` (database.py:53-58) is a stub with no implementation — no model imports or inherits from it, yet it is exported from the module — dead code that misleads future contributors — `placementops/core/database.py:53`
- F28 [code-quality] LOW (81): UUID PKs stored as `String(36)` across all ORM models — 97 `str()` wrapping calls in service code; Postgres should use native UUID for index efficiency — `placementops/core/models/patient_case.py:35`

### admin-surfaces (3 findings)

- F29 [api-contracts] HIGH (95): Route collision — outreach router registers 405 handlers on `GET/POST/PATCH /templates/outreach` which fire before the admin router's CRUD routes (registration order in `main.py:79-89`) — admin template create/update silently blocked by 405 response — `main.py:79`
- F30 [type-consistency] MEDIUM (95): Both modules define `TemplateListResponse` with different field names — outreach returns `{templates: [...]}`, admin returns `{items: [...], total: int}` — any frontend calling the same path gets different shapes depending on router registration order — `placementops/modules/outreach/schemas.py:120`
- F31 [api-contracts] MEDIUM (88): `GET /admin/organization` returns `updated_at=org.created_at` and `settings_json=None` — fabricated response values; PATCH echoes back request payload instead of persisted DB row — `placementops/modules/admin/router.py:312`

### intake-module (4 findings)

- F32 [error-handling] HIGH (88): `validate_import` (router.py:434) and `commit_import` (router.py:490) both do `await file.read()` (full unbounded read) before the 10 MB size check — memory exhaustion vector; upload endpoint correctly uses `file.read(MAX_UPLOAD_BYTES + 1)` but these endpoints regressed — `placementops/modules/intake/router.py:434,490`
- F33 [auth-security] HIGH (85): `assign_case` (service.py:736) fetches assigned user without org filter — `select(User).where(User.id == assigned_user_id)` allows cross-org user assignment; attacker can set cross-tenant user as coordinator, enabling enumeration by UUID — `placementops/modules/intake/service.py:736`
- F34 [spec-compliance] HIGH (85): Intake assign endpoint URL deviates from spec — router exposes `POST /cases/{id}/assign-coordinator` but AC6 test expects `POST /cases/{id}/assign`; no HTTP-level test exercises the actual deployed route — `placementops/modules/intake/router.py:257`
- F35 [error-handling] LOW (78): `check_zip_bomb()` does not catch `zipfile.BadZipFile` — a CSV where first two bytes are `0x50 0x4B` ("PK") causes unhandled 500 error instead of clean 400 — `placementops/modules/intake/service.py:123`

### clinical-module (2 findings)

- F36 [spec-compliance] HIGH (92): Clinical-module AC8 lists field names `trach, vent, wound_vac_needs, oxygen_required, memory_care_needed, behavioral_complexity_flag, bariatric_needs, iv_antibiotics, tpn, isolation_precautions` but ORM uses `accepts_trach, accepts_vent, accepts_wound_vac, accepts_oxygen_therapy, accepts_memory_care, accepts_behavioral_complexity, accepts_bariatric, accepts_iv_antibiotics, accepts_tpn, accepts_isolation_cases` — spec-to-ORM name mismatch for all 10 fields — `placementops/core/models/clinical_assessment.py:35`
- F37 [spec-compliance] MEDIUM (82): `AssessmentVersionEntry` data model in spec declares `version_sequence: integer` field — not implemented in ORM or response schema; versions ordered only by `created_at` — `placementops/core/models/clinical_assessment.py:17`

### outreach-module (1 finding)

- F38 [logic-bug] HIGH (87): Race condition in `approve_action` — two coordinators simultaneously approving different pending-approval actions for the same case will both read `is_first_approved=True` and both call `transition_case_status`; second call fails with 400 "invalid_transition" instead of silent success — `placementops/modules/outreach/service.py:611`

### outcomes-module (3 findings)

- F39 [api-contracts] MEDIUM (90): `POST /cases/{case_id}/status-transition` in outcomes router has no `response_model` and returns raw untyped dict — all other transition endpoints return `PatientCaseSummary`; OpenAPI schema shows `{}` for this endpoint — `placementops/modules/outcomes/router.py:188`
- F40 [test-quality] MEDIUM (78): `test_ac14_all_outcome_types_produce_audit_event` does not assert AuditEvent for `placed` outcome type — placed outcome's audit check is absent from both AC10 and AC14 tests — `placementops/modules/outcomes/tests/test_outcomes.py:686`
- F41 [spec-compliance] MEDIUM (76): AC7 test does not assert `case.current_status` is unchanged after `family_declined` recording — premature auto-close guard never explicitly verified — `placementops/modules/outcomes/tests/test_outcomes.py:488`

### auth-module (2 findings)

- F42 [test-quality] MEDIUM (78): Rate-limit test asserts `status_code in (401, 422)` but 422 is never correct for a valid JSON payload auth failure — 422 assertion is dead code masking potential validation errors — `placementops/modules/auth/tests/test_rate_limit.py:143`
- F43 [test-quality] LOW (76): RBAC test `read_only_has_minimal_permissions` uses `"read" in permission_string` substring check — non-semantic, can pass with unexpected permission strings — `placementops/modules/auth/tests/test_rbac.py:365`

### matching-module (1 finding)

- F44 [test-quality] MEDIUM (84): AC4 parametrize covers 12 of 13 clinical flag mappings — `in_house_hemodialysis` standalone case not in parametrize list; covered only by dual-check tests with combined preconditions — `placementops/modules/matching/tests/test_generate.py:256`

### analytics-module (1 finding)

- F45 [spec-compliance] MEDIUM (77): AC7 performance constraint (< 2000ms for 1000 cases) tested only against SQLite in-memory — SQLite has different performance characteristics from PostgreSQL target; performance constraint is structurally untested on real target — `placementops/modules/analytics/tests/test_analytics.py:63`

---

## Project-Level Findings (Manual Attention Required)

- NM1 [code-quality] HIGH (92): Inconsistent transaction ownership — `facilities` module commits in router; all other modules commit inside service layer. Makes multi-service atomic transactions impossible for facilities callers. Choose one pattern project-wide.
- NM2 [code-quality] MEDIUM (89): Divergent cancelable outreach state sets — `outreach/service.py:68` includes `"failed"` in `_CANCELABLE_STATES` frozenset; `outcomes/service.py:72` excludes it in `_CANCELABLE_OUTREACH_STATES` list. Difference is undocumented and list is mutable.
- NM3 [code-quality] MEDIUM (87): 8 per-module `conftest.py` files each replicate 100-250 lines of identical fixture boilerplate (engine, db_session, org, user, make_jwt). Should be a shared `placementops/modules/conftest.py`.
- NM4 [code-quality] MEDIUM (86): Inconsistent audit emission — `state_machine.py`, `matching/service.py`, `clinical/service.py`, `outreach/service.py` instantiate `AuditEvent` directly; `facilities/`, `admin/`, `outcomes/` use `emit_audit_event()` helper. Direct construction bypasses helper's parameter validation.

---

## Fixed (Prior Pass)

F1–F10 from sweep-2026-04-12T14-30-00Z: all resolved (admin prefix, route collision, auth header, OOM, closed-case guard, clinical fit score, shared types field names, form field names, invalid status string, LIKE injection).
